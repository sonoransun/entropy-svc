# systemd tmpfiles.d entry for the mixrand daemon runtime directory.
# Install to /usr/lib/tmpfiles.d/mixrand.conf and run
# `systemd-tmpfiles --create mixrand.conf` once at install time; systemd
# recreates /run/mixrand on every boot.
d /run/mixrand 0755 mixrand mixrand -
